import React from "react";
import {SelectWrapper  } from "./Select.styles";

export const Select = ({text, bgColor, fontColor}) => {
    return (
        <SelectWrapper >{text}</SelectWrapper>
    )
}